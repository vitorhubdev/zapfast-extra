# Omarchy palette fixtures

The TSV files record Catppuccin and Catppuccin Latte colors emitted by
`omarchy-theme-color --all`. JSON files are the expected rendering of
`contrib/omarchy/vespera.json.tpl`, including chat and bubble colors.
They exercise dark/light initialization without changing the host desktop.
