name: Flatpak

on:
  push:
    branches: [main]
    paths: [packaging/flatpak/**, .github/workflows/flatpak.yml, Cargo.lock, Cargo.toml]
  pull_request:
    paths: [packaging/flatpak/**, .github/workflows/flatpak.yml, Cargo.lock, Cargo.toml]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    container:
      image: ghcr.io/flathub-infra/flatpak-github-actions:freedesktop-25.08
      options: --privileged
    steps:
      - uses: actions/checkout@v7
      - name: Generate offline Cargo sources
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"
          python3 -m venv /tmp/flatpak-generator
          /tmp/flatpak-generator/bin/pip install aiohttp tomlkit PyYAML
          PATH="/tmp/flatpak-generator/bin:$PATH" python3 packaging/flatpak/test-packaging.py
          PATH="/tmp/flatpak-generator/bin:$PATH" packaging/flatpak/flathub.sh HEAD /tmp/vespera-flathub
          cp /tmp/vespera-flathub/cargo-sources.json packaging/flatpak/
      - uses: flatpak/flatpak-github-actions/flatpak-builder@v6
        with:
          manifest-path: packaging/flatpak/rocks.vespera.Vespera.yml
          bundle: vespera-test-x86_64.flatpak
          cache: false
