name: CI

on:
  push:
    branches: [main]
  pull_request:
  merge_group:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  quality:
    name: quality
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - name: GUI build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libxkbcommon-dev libwayland-dev libgl1-mesa-dev libasound2-dev cmake perl
      - uses: Swatinem/rust-cache@v2
      - name: Formatting
        run: cargo fmt --all --check
      - name: Clippy (default features)
        run: cargo clippy --locked --all-targets -- -D warnings
      - name: Clippy (all features)
        run: cargo clippy --locked --all-targets --all-features -- -D warnings
      - name: Rustdoc
        env:
          RUSTDOCFLAGS: -D warnings
        run: cargo doc --locked --all-features --no-deps

  test:
    name: test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 40
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
      - name: GUI build dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libxkbcommon-dev libwayland-dev libgl1-mesa-dev libasound2-dev cmake perl gnome-keyring dbus-x11
      - uses: Swatinem/rust-cache@v2
      # Archive restart tests use the same Secret Service backend as the app.
      # Run Linux tests inside a temporary D-Bus session with an unlocked
      # gnome-keyring so they exercise persistence instead of failing because
      # GitHub's image has no org.freedesktop.secrets provider.
      - name: Tests (default features)
        if: runner.os != 'Linux'
        run: cargo test --locked --all-targets
      - name: Tests (default features, Linux Secret Service)
        if: runner.os == 'Linux'
        run: |
          dbus-run-session -- bash -lc '
            echo -n "" | gnome-keyring-daemon --unlock --components=secrets
            cargo test --locked --all-targets
          '
      - name: Tests (all features)
        if: runner.os != 'Linux'
        run: cargo test --locked --all-targets --all-features
      - name: Tests (all features, Linux Secret Service)
        if: runner.os == 'Linux'
        run: |
          dbus-run-session -- bash -lc '
            echo -n "" | gnome-keyring-daemon --unlock --components=secrets
            cargo test --locked --all-targets --all-features
          '
      - name: Build macOS demo
        if: runner.os == 'macOS'
        run: cargo build --locked --features demo
      - name: macOS window smoke test
        if: runner.os == 'macOS'
        timeout-minutes: 3
        run: |
          target/debug/zapext --demo --demo-shot "$RUNNER_TEMP/zapfast-content.png" --demo-shot-delay 8000 &
          demo_pid=$!
          sleep 6
          screencapture -x "$RUNNER_TEMP/zapfast-window.png" || true
          wait "$demo_pid"
          test -s "$RUNNER_TEMP/zapfast-content.png"
      - name: macOS preview
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v7
        with:
          name: macos-preview
          path: ${{ runner.temp }}/zapfast-*.png

  windows-arm64:
    name: compile (Windows arm64)
    runs-on: windows-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v7
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-pc-windows-msvc
      - run: rustup target add aarch64-pc-windows-msvc
      - uses: Swatinem/rust-cache@v2
        with:
          key: windows-arm64
      - run: cargo check --locked --all-targets --all-features --target aarch64-pc-windows-msvc
